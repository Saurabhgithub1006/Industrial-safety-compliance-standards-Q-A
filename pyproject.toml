[project]
name = "safety-qa"
version = "0.1.0"
description = "Citation-grounded RAG Q&A over industrial safety standards"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
